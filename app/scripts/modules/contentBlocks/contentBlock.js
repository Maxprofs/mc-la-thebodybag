define(
	[
		'jquery',
		'tweenmax'
	],

	function(
		$,
		TweenMax
	) {

		'use strict';

		function ContentBlock(app, el) {
			if (arguments.length == 0) return;
			
			var app = app;
			var _this = this;
		}

		return ContentBlock;
	}
);